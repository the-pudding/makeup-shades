#foreward
  display flex
  justify-content center
  align-items center
  text-align center

  .slider-elements
    max-width $column-width
    margin 0 auto

  button
    background-color blue
    color white
    border-radius 0.25rem
    font-weight bold
    padding 0.5rem 1rem
    margin 0 auto 4rem
